#!/usr/bin/env bash
# emailcron: update not read email count
# @author touero
# @since 2025

CACHE_FILE="$HOME/.cache/email"
[ -f "$CACHE_FILE" ] || touch "$CACHE_FILE"

mbsync -a

shopt -s globstar
count=$(find "$HOME/Mail"/*/Inbox/new -type f | wc -l)
{
  echo "$count"
  echo "check update: $(date '+%Y-%m-%d %H:%M:%S')"
} >"$CACHE_FILE"

if [ "$count" == "0" ]; then
  exit 0
fi
notify-send "󰇮 Update email from remote" "There are $count unread emails"
