#!/usr/bin/env bash
# @author touero
# @since 2025
# pwd: copy a env password

[[ -f "$HOME/.xprofile" ]] && source "$HOME/.xprofile"
source $HOME/.local/bin/get_clipboard
CLIPBOARD=$(get_clipboard)

echo -n "$DOCKER_PWD" | $CLIPBOARD
